package dev.velometr.frontend

import androidx.compose.ui.graphics.Color

/** Matches the CSS custom properties in _docs/mileage-tracker-mockup.html. */
object VelometrColors {
    val background = Color(0xFF121615)
    val panel = Color(0xFF1A201E)
    val line = Color(0xFF2A322F)
    val accent = Color(0xFFE4572E)
    val contour = Color(0xFF3C4C43)
    val text = Color(0xFFEDEFEC)
    val textMuted = Color(0xFF8B958E)
    val textFaint = Color(0xFF57615B)
}
